function testLogicalOr(val) {
  // Only change code below this line

  if (val <= 9 || val > 20) {
return "Outside";
}


  // if (val <= 25 || val >= 21) {
  //   return "Outside";
  // }

  // Only change code above this line
  return "Inside";
}

testLogicalOr(15);